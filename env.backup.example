# Box Tracker Backup Configuration
# Copy this file to .env.backup and fill in your actual values

# Web server SSH connection details
WEBSERVER_USER=your-username
WEBSERVER_HOST=your-domain.com
# or WEBSERVER_HOST=123.456.789.123

# Local paths (on RPi)
LOCAL_DB_PATH=./data/dev.db
LOCAL_UPLOADS_PATH=./public/uploads
LOCAL_BACKUP_DIR=./backups

# Remote backup directory (on web server)
WEBSERVER_BACKUP_DIR=/home/your-username/box-tracker-backups
# or WEBSERVER_BACKUP_DIR=/var/backups/box-tracker

# Backup retention settings
KEEP_LOCAL_BACKUPS=true          # true/false - keep local copies after upload
KEEP_REMOTE_BACKUPS=30           # Number of remote backups to keep (0 = keep all)

# Example settings:
# KEEP_LOCAL_BACKUPS=false        # Save disk space on RPi
# KEEP_REMOTE_BACKUPS=30          # Keep 30 most recent backups on server